/*
Write a JavaScript function addNumbers that adds two numbers.

Examples:
addNumbers(2, 3) → 5
addNumbers(10, 5) → 15
*/

function addNumbers(a, b) {
  return a + b;
}

// Test cases
console.log(addNumbers(2, 3));   // 5
console.log(addNumbers(10, 5));  // 15
